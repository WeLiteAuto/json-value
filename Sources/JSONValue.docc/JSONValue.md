# ``JSONValue``

`JSONValue` is a type-safe representation of JSON-compatible values.

## Overview

Use `JSONValue` when you need to decode, store, and inspect dynamic JSON while
retaining Swift type safety through enum cases and typed accessors.

## Topics

### Core Type

- ``JSONValue``
